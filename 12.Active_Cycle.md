## 活跃周期 （Active Cycle）

在某个时钟周期内，如果一个 流式多处理器（Streaming Multiprocessor，简称 SM） 中至少有一个活跃的 warp（active warp） 处于驻留状态（resident），那么这个周期就被称为活跃周期。

在这个周期内，该 warp 可能是：可发射的（eligible） 或 停滞的（stalled）。

GPU 一般有很多个 SM，每个 SM 都会独立计算自己的 active cycles。
